# Solutions Stories

Бот режет картинку на стенку из сторис для профиля Telegram и делает
рамки для аватарок. Оплата — звёздами, без платёжного провайдера.

Бот: [@SolutionsStoriesbot](https://t.me/SolutionsStoriesbot) ·
соседний проект: [@emojimakerobot](https://t.me/emojimakerobot)

## Как это устроено

Сетка профиля Telegram выкладывает истории в три колонки: свежая —
слева сверху, старая — справа снизу. В превью от кадра 9:16 видно не
всё, а центральное окно 4:5. Значит, чтобы куски сошлись в полотно,
стыковать надо именно эти окна:

```
┌──────────┐   1080×1920 — сам кадр сторис
│   поля   │
├──────────┤   ← 285 px
│1080×1350 │   ← это окно видно в сетке профиля
├──────────┤
│   поля   │
└──────────┘
```

Отсюда всё остальное:

* частей всегда кратно трём — колонок в сетке ровно три;
* идеальная пропорция исходника считается, а не подбирается на глаз:
  `(3·4) : (rows·5)` — 6:5 для шести частей, 4:5 для девяти, 3:5 для
  двенадцати, 12:25 для пятнадцати;
* файлы отдаются **в порядке публикации**: первый — правый нижний угол,
  последний — левый верхний;
* поля вокруг окна заливаются размытым продолжением кадра (`FILL_MODE`),
  чтобы открытая целиком история не выглядела обрезком.

Рамки для аватарок рисуются кодом, а не лежат картинками: параметрический
венок масштабируется под любой размер и не тащит за собой папку PNG.
Настоящие рамки можно положить в `assets/frames/*.png` — они подхватятся
поверх процедурных.

Рамка по умолчанию **перекрашивается под выбранный фон**: тёмные места
рисунка уходят в тень акцента фона, средние — в сам акцент, блики — почти
в белый. Работа идёт по яркости, поэтому объём и светотень остаются на
месте, меняется только палитра. Кнопкой «🎨 Под фон» это выключается, если
нужны родные цвета рамки.

## Запуск

```bash
cp .env.example .env     # вписать BOT_TOKEN и ADMIN_IDS
pip install -r requirements.txt
python main.py
```

На Windows — `run.bat`: сам создаст venv и поставит зависимости.

Проверить картиночную часть без Telegram:

```bash
python selfcheck.py              # на синтетической шахматке
python selfcheck.py фото.jpg     # на своём фото
```

Результат — в `_selfcheck/`.

## Настройки

Всё в `.env`, см. `.env.example`.

| Переменная | Что делает |
|---|---|
| `BOT_TOKEN` | токен от @BotFather |
| `ADMIN_IDS` | кому открыта `/admin` |
| `CUT_PRICE` | цена нарезки в звёздах, одна на все сетки |
| `FRAME_PRICE` | цена рамки для аватарки |
| `PRICES` | `частей:звёзды` — если нужны разные цены за разные сетки |
| `REF_PER_FREE` | сколько активных рефералов = 1 бесплатная генерация |
| `WELCOME_CREDITS` | бесплатных генераций новичку |
| `FILL_MODE` | `blur` или `black` — поля вокруг окна |
| `DATA_DIR` | том под базу, если хостинг даёт отдельный |

## Файлы

| | |
|---|---|
| `main.py` | поллинг, роутеры, команды в меню |
| `handlers.py` | нарезка, рамки, рефералы, платежи |
| `admin.py` | статистика, топы, рассылка |
| `slicer.py` | геометрия сетки и нарезка |
| `frames.py` | венки и аватарки |
| `db.py` | SQLite: люди, генерации, платежи |
| `texts.py` | вся выдача бота |
| `keyboards.py` | кнопки |
| `selfcheck.py` | проверка картинок без Telegram |
| `make_assets.py` | сборка заставок в `assets/` |
| `make_avatar.py` | аватарка бота 512×512 |

## Оформление

Картинки-заставки кладутся в `assets/` и подхватываются по имени. Можно
нарисовать их где угодно — бот просто берёт файл:

| Файл | Где показывается |
|---|---|
| `welcome.jpg` | сразу после `/start` |
| `about.jpg` | кнопка «💡 Для чего это» |
| `manual.jpg` | кнопка «📘 Инструкция» |
| `frames.jpg` | кнопка «🖼 Рамки для авы» |

Формат любой: `welcome.jpg` и `frames.jpg` сейчас горизонтальные баннеры
16:9, `about.jpg` и `manual.jpg` — вертикальные карточки 4:5. Telegram
сжимает фото до 1280 по длинной стороне, больше смысла не имеет.
Файлов нет — бот отправит те же тексты без картинки и не упадёт.

`make_assets.py` рисует эти четыре карточки кодом, но **готовые файлы не
перезаписывает**: часть из них сделана руками, а сгенерировать их заново
скрипт не может. Перерисовать поверх — `python make_assets.py --force`.

Полноразмерные баннеры под рекламу лежат отдельно: `banner.jpg` и
`banner_frames.jpg`. `description.jpg` (640×360) — для BotFather.

### Свои рамки

Два пути:

* **прямо в боте** — `/admin` → «🖼 Рамки» → «Добавить рамку», отправить
  картинку **файлом**. Она уедет в `DATA_DIR/frames/`, то есть на том с
  базой: папка проекта перезаписывается на каждом деплое из Git, и
  загруженное рядом с кодом исчезло бы после первого обновления. Там же
  кнопка удаления;
* **в репозиторий** — положить PNG в `assets/frames/`.

Квадрат, рамка по краю, рабочий размер 1024×1024.

Возиться с ним не нужно — бот разбирается сам:

* **фон вырезается**, если прозрачности нет. Генераторы почти всегда
  отдают рамку на белом; цвет берётся из углов, так что чёрный фон
  тоже уйдёт. Если углы разного цвета, картинка считается фотографией
  и остаётся как есть;
* **размер фото подгоняется под дырку.** У одной рамки просвет на 45 %
  ширины, у другой на 70 % — аватарка масштабируется под конкретную,
  иначе одна налезает на лицо, а вокруг другой зияет подложка.

Единственное, за чем стоит следить, — контраст: чёрная рамка на фоне
«Чёрный» сольётся с ним, это не баг.

## Админка

`/admin` для тех, чьи id перечислены в `ADMIN_IDS`:

* **Статистика** — люди, генерации, звёзды, доля платящих, средний чек и
  выручка на человека;
* **По дням** — таблица за 14 дней: новые, генерации, платные, звёзды;
* **Топы** — по тратам и по приведённым людям;
* **Платежи** — последние 15 с суммой и тем, за что платили;
* **Найти человека** — по id или `@username`: когда пришёл, сколько
  резал, сколько потратил, кого привёл. Прямо в карточке кнопки
  `+1 / +3 / +10 / −1`;
* **Выдать** — одной строкой: `@username 3` начислить, `123456789 -1`
  списать, `@username =0` выставить баланс ровно. Каждая выдача пишется
  в журнал, человеку уходит уведомление;
* **Рассылка** — копией, с выбором «всем» или «активным за месяц»;
* **История рассылок** — сколько дошло, сколько заблокировали.
