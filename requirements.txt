# ВАЖНО: комментарии только на отдельных строках — некоторые хостинги
# читают файл построчно и передают строку в pip как есть.

# Платежи звёздами (XTR) без провайдера умеет Bot API 7.4+, в aiogram
# это 3.7. Берём заведомо свежее: на старых версиях answer_invoice
# требует provider_token и падает на пустом.
aiogram>=3.15,<4

aiosqlite>=0.20,<0.22
python-dotenv>=1.0,<2.0

# Нарезка кадров и рамки для аватарок.
Pillow>=10.0,<13
